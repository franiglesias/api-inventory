# Copy this file and change values that suit to your development setup
# cp .env.dist .env
# cp .env.dist .env.test

## ENVIRONMENT
# Node environment (development | production | test)
NODE_ENV=development

## SERVER
# Application port. For testing we recommend to change it to another value (like 3333)
PORT=3000

## PERSISTENCE

# Storage adapter selection: "memory" | "sqlite"
# In test you normally would prefer "memory", but if may want to use the sqlite adpater
STORAGE_ADAPTER=memory

# SQLite database file path (used when STORAGE_ADAPTER=sqlite)
# Default in Docker image is /data/inventory.db
SQLITE_DB_PATH=./data/inventory.db

# Initial data set for demos, tests, etc. Leave blank for tests
INITIAL_DATA=./data/products.json

## OPTIONAL VARIABLES

# Optional developer-friendly settings used by npm scripts
# See package.json scripts; safe defaults below
LOG_LEVEL=info
DEBUG=
NODE_OPTIONS=--enable-source-maps
